Use this directory by 
1.  Create a python virtual environment:
python -m venv <venv_name>
2. activate the environment:
source ./<venv_name>/bin/activate.fish (if you are using fish like me)
3. install the directory:
pip install -e .
4. add "animal." to imports 
